name: kiss_repository_example
description: "Example app showcasing kiss_repository with multiple implementations"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  kiss_repository:
    path: ../
  kiss_firebase_repository:
    path: ../../kiss_firebase_repository  
  kiss_pocketbase_repository:
    path: ../../kiss_pocketbase_repository
  firebase_core: ^3.13.1
  cloud_firestore: ^5.6.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true 